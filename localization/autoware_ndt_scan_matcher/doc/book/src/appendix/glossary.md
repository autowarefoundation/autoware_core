# Glossary

To be filled with one-line definitions. Planned terms:

- **NDT** — Normal Distributions Transform.
- **TP** — transform probability.
- **NVTL** — nearest-voxel transformation likelihood.
- **Voxel / leaf** — a grid cell and its fitted normal distribution (centroid + covariance).
- **kd-tree** — spatial index over voxel centroids for radius search.
- **Regularization** — optional longitudinal-distance pull toward a reference pose.
- **MatchScratch** — reused per-align workspace + last result.
- **ArcSwap / rcu** — the lock-free engine-state double-buffer.
- **WCET** — worst-case execution time.
- **Host ports** — `MapSource` / `OutputSink` / `Clock` portability traits.
- **TPE** — Tree-Structured Parzen Estimator (align-service search).
- **Track A / Track B** — the std/ROS build vs the `no_std`/kernel build.

> Status: outline (draft to be written).
